# ${{ values.name }}

This service was generated by the Production Web API golden path.

## Runtime contract

- `/health` proves the process is responsive.
- `/ready` proves the service is ready to receive traffic.
- `/v1/metadata` exposes non-sensitive service metadata.
