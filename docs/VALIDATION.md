# Validation

Run `make test`. Once the Backstage app exists, record exact Node/Backstage versions, start/build/test commands, live Catalog proof, Scaffolder task result, generated-service test output, and scorecard evidence. No screenshots unless captured from the running portal.
